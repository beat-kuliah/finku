# Dev: Vite proxies /api → http://localhost:8080 (see vite.config.ts)
VITE_API_URL=/api

# OAuth — leave empty to disable that provider (button hidden).
# Google: Web Client ID from Google Cloud Console (must match backend GOOGLE_CLIENT_ID).
VITE_GOOGLE_CLIENT_ID=
